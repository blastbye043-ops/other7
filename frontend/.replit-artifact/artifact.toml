kind = "web"
previewPath = "/"
title = "YTDown - YouTube Video Downloader"
version = "1.0.0"
id = "artifacts/ytdown"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25301

[services.development]
run = "pnpm --filter @workspace/ytdown run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ytdown", "run", "build" ]
publicDir = "artifacts/ytdown/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25301"
BASE_PATH = "/"
